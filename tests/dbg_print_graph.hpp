#pragma once

#include <string>
#include <iostream>

template<typename graph_t>
void dbgPrintGraph(graph_t& g, const std::string& inGraphName, bool inShowLinkTypes = true, bool inShowVertexIds = false) {
    //std::cout << "\n --- \n" << "# " << inGraphName << "\n";
    //g.print(std::cout);
    //std::cout << std::endl;
}
